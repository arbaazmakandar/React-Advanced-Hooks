import React, { useEffect, useRef } from "react"

const UseRef = () => {

    // Usecase 1
    // const counterRef = useRef(null);

    // const incrementCounter = () =>{
    //     counterRef.current++;
    //     console.log("Counter: ", counterRef.current)
    // }

    // console.log('Component Rerendered ')


    // Usecase 2

    //     const inputRef = useRef(null);

    //     useEffect(()=>{
    //         inputRef.current.focus(); // focus is the DOM property given to the element
    //     },[])

  return (<></>

// UseCase1
//     // <div><button onClick={incrementCounter}>Increment</button></div>


// UseCase2
//     <input ref={inputRef}/>
  )
}

export default UseRef